<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <title>Tim Scully</title>
    <style>
      @import url(https://fonts.googleapis.com/css?family=Boogaloo);
      @font-face {
        font-family: 'Boogaloo', arial, cursive;
      }
      body, #container-index {
        position: absolute;
        margin: 0;
        padding: 0;
        border: 0;
        width: 100%;
        max-height: 800px;
        background-color: #FF1493;
      }

      #container-index {
        position: absolute;
        margin: 0;
        padding: 0;
        border: 0;
        max-width: 100%;
        min-height: 800px;
        background-color: #FF1493;
        overflow:hidden;
      }

      .copy-nav {
        text-align: center;
        font-family: 'Boogaloo', arial, cursive;
        color: #ffffff;
        font-size: 2.3rem;
        line-height: 4rem;
        letter-spacing: 0;
        text-decoration: none;
        opacity: 0.75;
        transform: translateZ(0);
        box-shadow: 0 0 1px rgba(0, 0, 0, 0);
        backface-visibility: hidden;
        -moz-osx-font-smoothing: grayscale;
        transition-duration: 0.3s;
        transition-property: transform;
      }

      .copy-nav:hover  {
        text-align: center;
        cursor: pointer;
        opacity: 1;
        transform: scale(1.1);
      }

      #linkedin, #email-icon {
        padding-top:10px;
      }

      .copy-animation {
        font-family: 'Boogaloo', arial, cursive;
        color: #ffffff;
        font-size: 4rem;
        line-height: 5rem;
        letter-spacing: 0;
        text-decoration: none;
        margin: 20px 0 0 0;
      }

      #dev-image-container {
        position:relative;
        display: block;
        opacity: 0;
        margin-top: 60px;
      }

      #animation-container {
        margin: 0 auto;
        position: relative;
        display: block;
        width: 100%;
        height: auto;
      }

      #copy-1-container, #copy-2-container, #copy-3-container, #copy-4-container {
        position:absolute;
        opacity: 0;
        text-align: center;
        width: 100%;
        margin-top: 15px;
      }

      @media screen and (max-width: 600px) {

          #container-index {
            width: 100%;
          }

          .copy-nav {
            font-size: 1.8rem;
            line-height: 2rem;
            padding: 10px 0 0 0;
          }

          .copy-animation {
            font-size: 2.8rem;
            line-height: 3.2rem;
            padding: 20px 0 0 0;margin-top:70px;
            letter-spacing: 1px;
          }

          .fluid {
            width: 100%;
            height: auto;
          }

          #linkedin, #email-icon {
            padding-left:15px;
            padding-top: 7px;
          }

          #portfolio {
            padding-left:10px;
            padding-top: 10px;
          }

          #dev-image-container {
            position:relative;
            display: block;
            opacity: 0;
            margin-top: 10%;
          }
      }

      body, #container-portfolio {
        position: absolute;
        margin: 0;
        padding: 0;
        border: 0;
        width: 100%;
        max-height: 800px;
        background-color: #249CFF;
      }

      #container-portfolio {
        position: absolute;
        margin: 0;
        padding: 0;
        border: 0;
        max-width: 100%;
        min-height: 800px;
        background-color: #249CFF;
        overflow:hidden;
      }

      .copy-portfolio {
        font-family: 'Boogaloo', arial, cursive;
        color: #ffffff;
        font-size: 1.8rem;
        line-height: 3rem;
        letter-spacing: 0;
        text-decoration: none;
        opacity: 0.75;
        margin: 20px 0 0 0;
      }

      .copy-portfolio:hover {
        opacity: 1;
      }

      h3 {
        font-family: 'Boogaloo', arial, cursive;
        color: #ffffff;
        margin:0;
        padding:0;
        font-size: 2.6rem;
        line-height: 3.5rem;
        letter-spacing: 0;
        text-decoration: none;
        margin: 20px 0 0 0;
      }

      .hvr-rotate-1 {
        -webkit-transform: translateZ(0);
        transform: translateZ(0);
        box-shadow: 0 0 1px rgba(0, 0, 0, 0);
        -webkit-backface-visibility: hidden;
        backface-visibility: hidden;
        -moz-osx-font-smoothing: grayscale;
        -webkit-transition-duration: 0.3s;
        transition-duration: 0.3s;
        -webkit-transition-property: transform;
        transition-property: transform;
      }
      .hvr-rotate-1:hover, .hvr-rotate-1:focus, .hvr-rotate-1:active {
        -webkit-transform: rotate(2deg);
        transform: rotate(2deg) scale(1.3);
      }

      .hvr-rotate-2 {
        -webkit-transform: translateZ(0);
        transform: translateZ(0);
        box-shadow: 0 0 1px rgba(0, 0, 0, 0);
        -webkit-backface-visibility: hidden;
        backface-visibility: hidden;
        -moz-osx-font-smoothing: grayscale;
        -webkit-transition-duration: 0.3s;
        transition-duration: 0.3s;
        -webkit-transition-property: transform;
        transition-property: transform;
      }
      .hvr-rotate-2:hover, .hvr-rotate-2:focus, .hvr-rotate-2:active {
        -webkit-transform: rotate(-2deg);
        transform: rotate(-2deg) scale(1.3);
      }

      @media screen and (max-width: 600px) {

          #container-portfolio {
            width: 100%;
          }

          .copy-nav {
            font-size: 1.8rem;
            line-height: 2rem;
            padding: 10px 0 0 0;
          }

          .fluid {
            width: 100%;
            height: auto;
          }
      }
    </style>
  </head>
  <body>
    <table width="100%" border="0" align="center" cellpadding="0" cellspacing="0">
      <tr>
        <td valign="top" align="center">
          <%= yield %>
        </td>
      </tr>
    </table>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/1.18.2/TweenLite.min.js"></script>
    <script> window.javascript || document.write('<script js/TweenLite.min.js"><\/script>')</script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/1.18.2/plugins/CSSPlugin.min.js"></script>
    <script> window.javascript || document.write('<script src="js/CSSPlugin.min.js"><\/script>')</script>
    <script src="js/main.js" type="text/javascript" ></script>
    <script type="text/javascript">
      startAnimation();
    </script>
  </body>
</html>
